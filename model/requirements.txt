# Exact-pinned to versions verified working in CI + locally (May 2026).
# `==` over `>=` so a breaking upstream release can't silently tank a daily
# cron run. Upgrade deliberately — see scripts/upgrade_deps.sh.

# Data + numerics
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
pyarrow==21.0.0

# Modeling
xgboost==2.1.4
scikit-learn==1.6.1

# Ingest
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.1.1

# Storage / utils
python-dotenv==1.2.1
tqdm==4.67.3

# Testing (TDD)
pytest==8.4.2
pytest-cov==7.1.0
